---
const {
  title = "",
  background = "default",
  innerWidth = "normal",
} = Astro.props;

const maxWidth = innerWidth === "small" ? "max-w-[500px]" : "max-w-[1024px]";

const bgClass = {
  default: "bg-white",
  light: "bg-white",
  dark: "bg-gray-50",
  transparent: "bg-transparent",
}[background];
---

<section class={`w-full py-3 ${bgClass}`}>
  <div class={`w-full mx-auto px-4 md:px-6 py-3 ${maxWidth}`}>
    {title && <h3 class="text-xl font-semibold mb-3">{title}</h3>}
    <slot />
  </div>
</section>
